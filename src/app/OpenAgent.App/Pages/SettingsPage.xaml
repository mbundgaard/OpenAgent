<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:OpenAgent.App.ViewModels"
             x:Class="OpenAgent.App.Pages.SettingsPage"
             x:DataType="vm:SettingsViewModel"
             Title="Settings">
  <VerticalStackLayout Padding="24" Spacing="20">
    <Label Text="Server URL" FontAttributes="Bold" />
    <Label Text="{Binding ServerUrl}" />

    <Label Text="API token" FontAttributes="Bold" Margin="0,12,0,0" />
    <Label Text="{Binding TokenDisplay}" />
    <Button Text="Show / Hide" Command="{Binding ToggleRevealCommand}" />

    <Label Text="App version" FontAttributes="Bold" Margin="0,12,0,0" />
    <Label Text="{Binding AppVersion}" />

    <Button Text="Reconfigure agent connection"
            Command="{Binding ReconfigureCommand}"
            BackgroundColor="#FF3B30" TextColor="White"
            Margin="0,24,0,0" />
  </VerticalStackLayout>
</ContentPage>
